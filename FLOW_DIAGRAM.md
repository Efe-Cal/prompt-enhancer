# User Question Flow Diagram

## Complete Request Flow with User Dialog

```
┌─────────────────────────────────────────────────────────────────────┐
│                        1. USER INITIATES                             │
│                                                                      │
│  User fills form → Clicks "Enhance" button                          │
└────────────────────────────┬─────────────────────────────────────────┘
                             │
                             ↓
┌─────────────────────────────────────────────────────────────────────┐
│                    2. FRONTEND → BACKEND                             │
│                                                                      │
│  HTTP POST /api/enhance/                                             │
│  {                                                                   │
│    task: "...",                                                      │
│    lazy_prompt: "...",                                               │
│    use_web_search: true                                              │
│  }                                                                   │
└────────────────────────────┬─────────────────────────────────────────┘
                             │
                             ↓
┌─────────────────────────────────────────────────────────────────────┐
│                    3. DJANGO API CREATES TASK                        │
│                                                                      │
│  1. Generate task_id (UUID)                                          │
│  2. Start Celery task: enhance_prompt_task.delay(...)               │
│  3. Return: { task_id: "abc-123", status: "processing" }            │
└────────────────────────────┬─────────────────────────────────────────┘
                             │
              ┌──────────────┴──────────────┐
              │                             │
              ↓                             ↓
┌──────────────────────────┐    ┌──────────────────────────┐
│  4a. FRONTEND            │    │  4b. CELERY WORKER       │
│                          │    │                          │
│  Connect WebSocket:      │    │  Task starts running:    │
│  ws://.../enhance/       │    │  - Call OpenAI API       │
│      abc-123/            │    │  - Process LLM response  │
│                          │    │  - Handle tool calls     │
└────────┬─────────────────┘    └──────────┬───────────────┘
         │                                  │
         │ WebSocket connection established │
         │◄─────────────────────────────────┤
         │                                  │
         │                                  ↓
         │                       ┌────────────────────────┐
         │                       │  5. LLM NEEDS INPUT    │
         │                       │                        │
         │                       │  Celery task calls:    │
         │                       │  ask_user_question()   │
         │                       └───────────┬────────────┘
         │                                   │
         │                                   ↓
         │                       ┌────────────────────────┐
         │                       │  6. SEND VIA REDIS     │
         │◄──────────────────────┤                        │
         │  WebSocket message    │  Channel Layer sends:  │
         │  {                    │  group_send(           │
         │    type: "user_q..",  │    "enhance_abc-123",  │
         │    question: "..."    │    user_question event │
         │  }                    │  )                     │
         └───────┬───────────────┴────────────────────────┘
                 │
                 ↓
┌─────────────────────────────────────────────────────────────────────┐
│                     7. FRONTEND SHOWS DIALOG                         │
│                                                                      │
│  ┌───────────────────────────────────────────┐                      │
│  │  Question from AI                         │                      │
│  │  ───────────────────────────────────────  │                      │
│  │  What tone should this prompt have?       │                      │
│  │                                            │                      │
│  │  [ Your answer here...              ]     │                      │
│  │                                            │                      │
│  │          [Submit]  [Cancel]                │                      │
│  └───────────────────────────────────────────┘                      │
│                                                                      │
│  User types answer: "formal and professional"                       │
└────────────────────────────┬─────────────────────────────────────────┘
                             │
                             ↓
┌─────────────────────────────────────────────────────────────────────┐
│                  8. SEND ANSWER BACK                                │
│                                                                      │
│  WebSocket.send({                                                    │
│    type: "user_answer",                                              │
│    answer: "formal and professional"                                 │
│  })                                                                  │
└────────────────────────────┬─────────────────────────────────────────┘
                             │
                             ↓
┌─────────────────────────────────────────────────────────────────────┐
│                  9. CONSUMER STORES ANSWER                           │
│                                                                      │
│  WebSocket Consumer receives answer                                  │
│  → Stores in Redis cache:                                            │
│     cache.set("user_answer_abc-123", "formal and professional")     │
└────────────────────────────┬─────────────────────────────────────────┘
                             │
                             ↓
┌─────────────────────────────────────────────────────────────────────┐
│                  10. CELERY TASK CONTINUES                           │
│                                                                      │
│  ask_user_question() polls Redis cache                               │
│  → Finds answer: "formal and professional"                           │
│  → Returns to task                                                   │
│  → Task continues processing with user's answer                      │
│  → Completes LLM request                                             │
└────────────────────────────┬─────────────────────────────────────────┘
                             │
                             ↓
┌─────────────────────────────────────────────────────────────────────┐
│                  11. SEND RESULT BACK                                │
│                                                                      │
│  Task calls:                                                         │
│  channel_layer.group_send(                                           │
│    "enhance_abc-123",                                                │
│    { type: "task_complete", result: "Enhanced prompt..." }           │
│  )                                                                   │
└────────────────────────────┬─────────────────────────────────────────┘
                             │
                             ↓
┌─────────────────────────────────────────────────────────────────────┐
│                  12. FRONTEND DISPLAYS RESULT                        │
│                                                                      │
│  WebSocket receives:                                                 │
│  { type: "task_complete", result: "..." }                            │
│                                                                      │
│  → Hide loading spinner                                              │
│  → Display enhanced prompt in right panel                            │
│  → Close WebSocket connection                                        │
│  → Done! ✅                                                          │
└─────────────────────────────────────────────────────────────────────┘
```

## Key Components

### Redis Roles:
1. **Celery Broker** - Task queue for async jobs
2. **Channel Layer** - WebSocket message routing
3. **Cache** - Temporary storage for user answers

### WebSocket Events:
- `user_question` - Celery → Frontend (show dialog)
- `user_answer` - Frontend → Celery (submit answer)
- `task_complete` - Celery → Frontend (show result)
- `task_error` - Celery → Frontend (show error)

### Async Operations:
- ✅ Non-blocking: User can browse while task runs
- ✅ Real-time: Updates appear instantly
- ✅ Interactive: Can ask questions mid-processing

## Demo Test Flow

To test the complete flow:

1. **Uncomment demo in** [`backend/api/tasks.py`](backend/api/tasks.py):
   ```python
   user_preference = ask_user_question(
       task_id=task_id,
       question="What tone should the enhanced prompt have?"
   )
   ```

2. **Start all services** (Redis, Celery, Django, Frontend)

3. **Click "Enhance"** in the UI

4. **Watch for dialog** to appear after ~2 seconds

5. **Type answer** (e.g., "formal")

6. **Submit** → Task continues and completes

7. **See result** in right panel

That's it! 🎉
